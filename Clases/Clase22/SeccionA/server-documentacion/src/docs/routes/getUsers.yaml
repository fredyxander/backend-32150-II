# get all users
paths:
  /users:
    get:
      summary: get all users
      tags:
        - Usuarios
      responses:
        "200":
          description: get all users successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"